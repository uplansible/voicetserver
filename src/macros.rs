// Phase 4 stub — spoken macro expansion
//
// Planned: config/macros.json maps spoken triggers to full text snippets.
// Expansion happens before WebSocket response, so the client receives
// already-expanded text.
//
// Review mode: {"type":"review","text":"..."} triggers a browser diff overlay;
// user confirms before text is injected (Shift+stop).
//
// Implement fully in Phase 4 after Phase 2 session vocab is complete.
